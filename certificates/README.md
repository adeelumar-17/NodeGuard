# Certificates

This directory contains SSL/TLS certificates for secure VPN communication.

Place your certificate files here:
- `server.crt` - Server certificate
- `server.key` - Server private key
- `ca.crt` - Certificate Authority certificate (optional)